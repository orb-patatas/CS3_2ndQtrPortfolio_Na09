A. Using universal selector
1. **What does it mean to have a list of font names when you specify a font-family?**
- When the first font written isn't available, the next font would apply
2. **What is the difference between border-box and content-box?**
- Content-box: W & H you set apply only to the content area of the element; default value
- Border-box: contnet + padding + border

B. Using the element selector for the body box:
1. What's the short-hand notation of the background CSS property?
- element {
  background: [color] [image] [position] / [size] [repeat] [attachment] [origin] [clip];
}

C. Using group selector
1. Give the other two ways to give colors in CSS with correct format.
2. How do you control the color opacity in CSS?

E. Class Selector
1. Whats the meaning of em unit of measurement?
2. Em vs Rem

F. Child Selector
1. What's vw and vh?
2. How's it different from using %?
3. Compare relative size units (%, vw) against fixed size units (px, pt, cm)

I. Using :hover pseudo-class
1. What are the pseudo-classes associated with a text-link other than :hover?
2. What's the simple rule when you use them all in one style rule?